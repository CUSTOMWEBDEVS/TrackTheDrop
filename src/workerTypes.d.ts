export type FrameMsg={type:'frame';bitmap:ImageBitmap;width:number;height:number;params:{thr:number;sens:number;morph:number;debugHeat:boolean;showMask:boolean;}};export type ResultMsg={type:'result';bitmap:ImageBitmap};